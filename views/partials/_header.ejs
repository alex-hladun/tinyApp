<header>

  <nav class="navbar navbar-expand-md navbar-dark bg-primary">
    <div class="navbar-collapse collapse w-100 order-1 order-md-0 dual-collapse2">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <a class="nav-link" href='/urls'>My URL's</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/urls/new">Create New URL</a>
        </li>
      </ul>
    </div>
    <div class="mx-auto order-0">
      <a class="navbar-brand mx-auto" href='/urls'>TinyApp</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".dual-collapse2">
        <span class="navbar-toggler-icon"></span>
      </button>
    </div>
    <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">
      <ul class="navbar-nav ml-auto">
        <% if(username) { %>
        <form class="form-inline" method="POST" action="/logout">
          <li class="nav-item">
            <span class="navbar-text mr-2">Logged in as <%= username %></span>
          </li>
          <li class="nav-item">
            <button class="btn btn-dark ml-auto" type="submit">Logout</a>
          </li>
        </form>
        <% } else { %>
        <form class="form-inline" method="POST" action="/login">
          <input class="nav-item form-control" name="username" placeholder="Username">
          <button class="nav-item btn btn-primary" type="submit">Sign in</a>
        </form>
        <% } %>
      </ul>
    </div>
  </nav>
</header>